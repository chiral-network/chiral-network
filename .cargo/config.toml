# MSVC debug linking is currently hitting LNK1318 PDB failures in this repo.
# Disabling linker-generated PDB output keeps local debug builds working.
[target.x86_64-pc-windows-msvc]
rustflags = ["-C", "link-arg=/DEBUG:NONE"]
