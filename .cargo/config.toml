# No custom linker flags — use default MSVC settings.
# Previous /DEBUG:NONE caused WDAC blocks (os error 4551).
# /DEBUG:FULL caused E0463 compilation failures.
# Default MSVC behavior produces valid PE binaries without issues.
