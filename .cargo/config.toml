[target.x86_64-pc-windows-msvc]
# Use /DEBUG:FULL instead of /DEBUG:NONE to keep PE debug directory intact.
# /DEBUG:NONE strips metadata that Windows Application Control (WDAC) needs.
rustflags = ["-C", "link-arg=/DEBUG:FULL"]
