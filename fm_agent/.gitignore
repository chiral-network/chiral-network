# Per-function source files are deterministically regenerated by
# `python3 fm_agent/extractor.py`. Skip them — committing 684 derived
# files just to delete and re-add them on every refactor adds noise.
extracted_functions/
